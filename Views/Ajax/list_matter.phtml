<div class ="col-md-3">
    <form method="post" action="#" class="post-add-matter">
        <input type="hidden" name="formation" value="<?= $id_formation; ?>">
        <select name="matter" class="form-control select-<?= $id_formation; ?>">
<?php

    foreach ($coll_matter as $matter){
        
        $notRefpedago = true;
        
        foreach ($coll_refpedago as $refpedago) {
            
            if( $refpedago->getData('matters_id') == $matter->getData('id') ){
                
                $notRefpedago = false;
                
            }
            
        }
        
        if( $notRefpedago ):          
?>
            <option class="option-<?= $matter->getData('id'); ?>" value="<?= $matter->getData('id'); ?>"><?= $matter->getData('title'); ?></option>
<?php   
        endif;
    }
?>
        </select>
        <button name="submit">Ajouter cette matière</button>
    </form>
</div>